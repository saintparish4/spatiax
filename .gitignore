# Rust build artifacts
/target/
**/*.rs.bk
*.pdb

# Working notes and scratch space — not part of the shipped repo
/base/
.claude/
CLAUDE.md

# Benchmark output
/criterion/
/flamegraph.svg
perf.data*

# Captured CAN logs and telemetry samples (can be large; keep fixtures explicit)
*.blf
*.mf4
*.ld
# ...except the golden file the export test compares against byte for byte
!/fixtures/*.ld
/logs/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# Local Python environment for the cantools differential oracle
/.venv/

# What the README's demo commands leave behind when run from the checkout
/lap.csv
/lap.png
